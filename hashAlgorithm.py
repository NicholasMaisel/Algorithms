import LinkedList

ARRAY_LENGTH = 255


class HashTable():
    def __init__(self,chainLength):
        self.chainLength = chainLength
        self.table = {}

    def hash(key):
        return(key%chainLength)

    def get(key):
        


    def put(key):
        if a.get(self.hash(key)):
            a.get(self.hash(key)).AddToFront(key)





def main():
    myHashTable = HashTable(ARRAY_LENGTH)
